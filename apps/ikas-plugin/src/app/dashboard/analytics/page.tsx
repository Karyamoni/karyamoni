import { MetricsDisplay } from "@/components/MetricsDisplay";

export const metadata = { title: "Analytics — Karyamoni IKAS Plugin" };

export default function AnalyticsPage() {
  return <MetricsDisplay />;
}
